1. 修改 nocos 版本为 v2.4.2(两个地方)
2. 修改源码 配置中心的 nacos 地址
3. 修改 jdk maven nodejs npm

当前终端执行
export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
export PATH=$JAVA_HOME/bin:$PATH

4. 修改脚本中的 redis 配置
6379 对应的 localhost 改为 ruoyi-redis
4. 修改脚本中的 mysql 配置
3306 对应的 localhost 改为 ruoyi-mysql