1. 安装 docker , docker compose

2. 修改 nocos 版本为 v2.4.2(两个地方)

3. 修改源码 配置中心的 nacos 地址(将 127.0.0.1:8848 修改为 ruoyi-nacos:8848)

4. 修改 sql 脚本(也就是 nacos 里的配置)
   redis 配置:
   6379 对应的 localhost 改为 ruoyi-redis
   mysql 配置:
   3306 对应的 localhost 改为 ruoyi-mysql

5. 安装 jdk maven nodejs npm

6. 编译服务器
   进入 bin_mac
   运行 ./all.sh

7. 编译客户端
   进入 ruoyi-ui\bin_mac
   运行 ./all.sh

8. 拷贝文件 ./copy.sh

9. 运行脚本
   ./deploy.sh base
   ./deploy.sh modules

10. 地址
    nacos 后台:
